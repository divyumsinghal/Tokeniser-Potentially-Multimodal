#pragma once
#include <string>
#include <vector>

namespace utils {

std::string invalid_token = "<UNK>";  // Default invalid token
int invalid_token_id = -1;            // Default invalid token ID

}  // namespace utils